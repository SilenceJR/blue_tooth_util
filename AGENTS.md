# blue_tooth_util 仓库规则

## 仓库边界

- 本仓库只负责 BLE Transport、协议 Adapter、戒指 Session、`.rota` 解析和 OTA 传输状态机。
- 不修改父级 Sublinur App 仓库，不实现 App 页面、网络下载、GetX Coordinator 或服务端接口。
- 不提交真实 `.rota`、`ota_tool.py`、AES 密钥、签名密钥、IV、设备隐私数据或产线资料。
- 保留用户和其他任务的改动；只暂存、提交所负责阶段的文件。

## OTA 协议约束

- Service UUID 和广播名称只用于筛选候选设备；必须使用 Manufacturer Data 派生的 MAC 确认目标戒指。
- `OTA_COMPLETE` 只表示 Bootloader 传输阶段完成；成功必须在重启后重连业务模式，并通过 `0x0402` 确认目标固件版本。
- 重试和恢复使用有上限的迭代状态机，不递归调用；进度按确认字节计算并限流，不逐包通知上层。
- 不为未确认的固件差异编写临时协议兼容层。
- AES-CCM 格式、原生后台服务或公开接口大改必须先输出方案、影响和回退方式，等待用户确认后再实施。

## 代理编排

- 主任务使用 `gpt-5.6-sol` 和 `high` 推理，对架构、实现、diff、验证和提交负责。
- Codex 配置允许最多 2 个 spawned-agent threads；本仓库流程进一步限制主任务一次最多运行一个子代理，所有子任务串行交接，子代理不得继续创建子代理。
- 每个功能阶段按“只读协议或调用链分析 → 主任务实现 → 测试代理补充测试 → 主任务复核 diff → 只读 Reviewer 审查 → 提交”执行。
- 子代理任务必须写明目标、文件所有权、只读或读写边界、禁止触碰的目录、验收标准和返回证据。
- 测试代理只修改主任务明确分配的测试文件和合成夹具，不修改生产代码；分析和 Reviewer 代理不得修改文件。

## 实现和文档

- 修改前读取真实入口、邻近实现和测试，保持 `BleTransport`、Adapter、Session 分层。
- 公共接口、参数、状态和不直观的协议意图使用简洁中文 DartDoc；标识符保持英文。
- 公共 API、依赖、行为、修复或真机边界变化时，同步 README、OTA 文档和 `CHANGELOG.md`；内部配置工作不写 Changelog。
- 文档必须区分协议定义、代码实现、构建通过、模拟器验证和 Android/iPhone 真机验证。

## 验证和提交

- 只对所负责阶段改动的 Dart 文件运行格式化，不对整个仓库执行批量格式化。
- 按阶段运行定向静态分析和包级 `flutter test`；仓库基线问题单独报告，不顺手扩大修复范围。
- 仓库配置认知复杂度阈值后执行对应检查；没有配置时明确记录不适用。
- BLE、MTU、无响应写、断连恢复和 OTA 成功只能由 Android 与 iPhone 真机证据确认；构建或模拟器不能替代真机结论。
- 每个阶段独立提交。提交前复核 staged diff，不自动合并或推送。
- 阶段报告列出 commit ID、公开 API、测试、构建、真机边界、文档状态和 App 可依赖内容。
